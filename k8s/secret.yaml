# TEMPLATE ONLY — placeholder values. Applying this file overwrites the real secret.
# Create the real secret out-of-band instead, e.g.:
#   kubectl -n billing create secret generic billing-db-secret \
#     --from-literal=DB_USER=... --from-literal=DB_PASSWORD=... --from-literal=DB_NAME=...
# `make k8s-apply` intentionally skips this file.
apiVersion: v1
kind: Secret

metadata:
  name: billing-db-secret
  namespace: billing
  labels:
    app.kubernetes.io/name: billing-api
    app.kubernetes.io/part-of: billing-app

type: Opaque

stringData:
  DB_USER: change-me
  DB_PASSWORD: change-me
  DB_NAME: billing_db
